# default rules
homepage:
  url:   /
  param: { module: default, action: index }

default_symfony:
  url:   /symfony/:action/*
  param: { module: default }

default_index:
  url:   /:module
  param: { action: index }

default:
  url:   /:module/:action/*

  
users:
  url:   /users/:action/*
  param: { module: users }

role:
  url:   /role/:action/*
  param: { module: role }

question:
  url:   /question/:action/*
  param: { module: question }

questioncategory:
  url:   /questioncategory/:action/*
  param: { module: questioncategory }      